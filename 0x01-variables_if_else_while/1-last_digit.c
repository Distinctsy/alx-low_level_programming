#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - annoying
 *
 * Reture: 0
 */
int main(void)
{
	int n;
	int x;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if (x > 5)
	{
		printf("and is greater than 5")
	}
	if (x==0)
	{
		printf("and is op)
	}
	return (0);
}
